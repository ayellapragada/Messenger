import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  View
} from 'react-native';

import Root from './root';

AppRegistry.registerComponent('Messenger', () => Root);
